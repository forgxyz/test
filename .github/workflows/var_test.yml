name: var_test

on:
  workflow_dispatch:

env:
  HELLO_WORLD: "${ ${{ secrets.HELLO_WORLD }}:-${{ vars.HELLO_WORLD }} }"



jobs:
  run_var_test_jobs:
    runs-on: ubuntu-latest
    environment: 
      name: var_test

    steps:
      - uses: actions/checkout@v3

      - name: test parameter expansion
        run: |
          echo $HELLO_WORLD
